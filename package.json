{
  "name": "bettr-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && node --test tests/web_state.test.mjs",
    "test:frontend": "vitest run",
    "test:frontend:watch": "vitest"
  },
  "devDependencies": {
    "@testing-library/dom": "latest",
    "jsdom": "latest",
    "vitest": "latest"
  }
}
